`use client`;

import type { FC } from "react";
import CategoryForm from "./CategoryForm";

const Dashboard: FC = () => {
  return (
    <main>
      <CategoryForm />
    </main>
  );
};

export default Dashboard;
